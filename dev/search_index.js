var documenterSearchIndex = {"docs":
[{"category":"section","location":"plans/#The-plan-interface","page":"The plan interface","text":"Every planner in VkFFT.jl returns an AbstractFFTs.Plan, so mul!, *, inv, \\, ldiv!, size, adjoint, AbstractFFTs.fftdims and AbstractFFTs.plan_inv all work. These methods belong to AbstractFFTs, Base and LinearAlgebra, so they have no docstrings of their own and are documented down here instead.","title":"The plan interface"},{"category":"section","location":"plans/#Specify-VkFFT","page":"The plan interface","text":"Use VkFFT.plan_fft, not plan_fft. A bare fft(x) or plan_fft(x) on a CuArray goes to CUFFT, and on a CLArray or MtlArray to whatever its owner provides.\n\nAbstractFFTs.plan_fft(x, region) is one generic function that the whole ecosystem dispatches on, and CUDA.jl already owns that dispatch for CuArray. Overwriting this would be type piracy which would make the order in which you load the module what decides which transform runs. This also breaks precompilation.","title":"Specify VkFFT"},{"category":"section","location":"plans/#Traits","page":"The plan interface","text":"x = rand(ComplexF32, 8, 6, 4) # 3D array of size 8×6×4\nx = CLArray(x) # MtlArray(x) for Metal, CuArray(x) for CUDA\np = VkFFT.plan_fft(x, (1, 3))\n\nsize(p)                       # (8, 6, 4), the input size\nsize(p, 2)                    # 6\nndims(p)                      # 3\nlength(p)                     # 192\neltype(p)                     # ComplexF32\nAbstractFFTs.fftdims(p)       # (1, 3), sorted\nAbstractFFTs.output_size(p)   # (8, 6, 4)\n\nsize is the input size and AbstractFFTs.output_size the output size. The two differ only for the real family, where the halved dimension shortens. eltype is the input element type, which for an inverse real plan is the complex one.\n\nPlan types are concrete. A region given as a tuple, an integer or left at its default carries its length in the type, so the whole plan type is inferable and applying it needs no dynamic dispatch. A region given as a range only gets its length at run time, which costs that one type parameter and nothing else.","title":"Traits"},{"category":"section","location":"plans/#Applying-a-plan","page":"The plan interface","text":"using LinearAlgebra\n\ny = p * x     # out-of-place, allocates the output\nmul!(y, p, x) # the same transform into a buffer you own\n\nFor an in-place plan, p * x returns x and mul!(y, p, x) demands y === x. For an out-of-place plan the two arrays have to be distinct.\n\nThe array reaching mul! need not be the one the plan was built from, only the same size, element type, layout and device.","title":"Applying a plan"},{"category":"section","location":"plans/#Inverses","page":"The plan interface","text":"inv(p) * y # the inverse plan, applied\np \\ y      # the same thing\n\nout = similar(x)\nldiv!(out, p, y) # ... into a buffer you own\n\nAbstractFFTs.plan_inv(p) # the inverse plan itself, not memoized\n\ninv memoizes its answer on the plan, so inv(p) === inv(p). Both routes come out of the plan cache, so neither compiles a second set of kernels.\n\nA forward plan inverts to VkFFT's in-kernel normalized inverse, one kernel launch instead of a transform plus a scaling pass, and it inverts straight back to the forward plan. The real-to-real family is the same, since VkFFT applies the exact per-type round-trip factor in-kernel.\n\nOne case needs an AbstractFFTs.ScaledPlan: the inverse of a plan_bfft is a normalized forward transform, and VkFFT's normalization only exists on the inverse direction.\n\nZero-padded plans, convolution plans and plans from VkFFT.unsafe_plan have no inverse, and each says why.","title":"Inverses"},{"category":"section","location":"plans/#Adjoints","page":"The plan interface","text":"p'          # the adjoint plan\n(p')' === p # true\nsize(p')    # AbstractFFTs.output_size(p)\n\np' satisfies dot(v, p * x) == dot(p' * v, x), and for the real families it satisfies it in the inner product a real-linear transform is self-adjoint under, which pairs real and imaginary parts separately. Zero-padded plans and convolution plans have no adjoint, and say so.","title":"Adjoints"},{"category":"section","location":"plans/#Scaling","page":"The plan interface","text":"alpha * plan and plan * alpha give an AbstractFFTs.ScaledPlan:\n\nscaled = 3 * p\nscaled.scale      # Float32(3), not Int(3)\ninv(scaled).scale # Float32(1/3)\n\nrotated = im * p\nrotated.scale    # ComplexF32(im)\n\nThe scale is narrowed to the plan's own real precision.","title":"Scaling"},{"category":"section","location":"plans/#The-plan-cache","page":"The plan interface","text":"Plans are cached and reused, so planning the same transform twice is free:\n\nVkFFT.plan_fft(x, (1, 2)) === VkFFT.plan_fft(x, (2, 1)) # true\n\nVkFFT.cache_size()   # how many plans the cache holds\nVkFFT.clear_cache!() # drop them all\n\nThe key is the backend, the device context, the input element type, the input size, the canonicalized region, the direction, whether the plan normalizes, whether it is in-place, whether it is real-to-complex, the real length of the halved dimension, the DCT and DST types, the zero-padded range, and the two tuning knobs. The array's identity is not in it, so a plan built from one buffer is returned for another of the same shape on the same device.\n\nThe cache is unbounded and cleared only by VkFFT.clear_cache!. Concurrent planning of one shape is serialized, and every task gets the same plan back. Convolution plans and plans from VkFFT.unsafe_plan are never cached.\n\nClearing the cache lets the plans in it be finalized. See Freeing a plan.","title":"The plan cache"},{"category":"section","location":"plans/#Printing","page":"The plan interface","text":"VkFFT.plan_fft(x, (1, 3))\n# 2D out-of-place complex forward VkFFT plan for 8×6×4 ComplexF32 on dims (1, 3) [opencl]\n\nVkFFT.plan_ifft(x, 2)\n# 1D out-of-place complex normalized inverse VkFFT plan for 8×6×4 ComplexF32 on dim (2,) [opencl]\n\nVkFFT.plan_fft(x, ())\n# trivial out-of-place complex forward VkFFT plan for 8×6×4 ComplexF32 on dims () [opencl]\n\nThe leading count is the number of transformed dimensions, not ndims. A trivial plan transforms nothing and applies as a copy. The tag in brackets is the backend.","title":"Printing"},{"category":"section","location":"benchmarks/#Benchmarks","page":"Benchmarks","text":"The numbers are not here yet.\n\nThe plan is to measure VkFFT against CUFFT on an NVIDIA card, and against FFTW on the host for the same shapes, over the size classes the test suite already covers: powers of two, small primes, mixed radix, a Bluestein length that fits in one upload and one that does not. Complex-to-complex, real-to-complex and real-to-real, at each precision the backend runs, in-place and out of place, with the plan built once and applied many times so that planning cost and transform cost are reported separately.\n\nTwo comparisons matter more than the raw throughput. The first is prime and near-prime lengths, since avoiding a performance cliff there is one of the reasons to use VkFFT at all. The second is the normalized inverse, which VkFFT applies inside the transform kernels instead of in a second pass, so an ifft should cost one kernel launch where a scaled bfft costs two.\n\nThe autotuner gets its own numbers: what Tuning buys over VkFFT's defaults, per shape and per device. On pocl's CPU device the answer so far is nothing measurable, which says nothing about a GPU.","title":"Benchmarks"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"Every entry point is called as VkFFT.plan_fft, never as a bare plan_fft. See Specify-VkFFT.\n\nBase.:*, LinearAlgebra.mul!, Base.inv, \\, ldiv!, Base.size, adjoint, AbstractFFTs.fftdims, AbstractFFTs.plan_inv, AbstractFFTs.adjoint_mul and Base.show have no docstrings of their own. They are documented on The plan interface.","title":"API reference"},{"category":"section","location":"api/#Plan-types","page":"API reference","text":"","title":"Plan types"},{"category":"section","location":"api/#Complex-to-complex-transforms","page":"API reference","text":"","title":"Complex-to-complex transforms"},{"category":"section","location":"api/#Real-to-complex-transforms","page":"API reference","text":"","title":"Real-to-complex transforms"},{"category":"section","location":"api/#Real-to-real-transforms","page":"API reference","text":"","title":"Real-to-real transforms"},{"category":"section","location":"api/#Convolution","page":"API reference","text":"","title":"Convolution"},{"category":"section","location":"api/#The-raw-configuration","page":"API reference","text":"","title":"The raw configuration"},{"category":"section","location":"api/#Freeing,-caches-and-tuning-records","page":"API reference","text":"","title":"Freeing, caches and tuning records"},{"category":"section","location":"api/#Errors","page":"API reference","text":"","title":"Errors"},{"category":"type","location":"api/#VkFFT.VkFFTPlan","page":"API reference","text":"VkFFTPlan{T, N, IP, B, M}\n\nA first-class AbstractFFTs.Plan backed by a VkFFT application.\n\nThe type parameters are (T) the element type, (N) ndims of the input array, (IP) whether the plan is in-place, (B) the backend tag (:cuda, :opencl or :metal) and (M) the number of transformed dimensions. app is C_NULL for a plan whose region transforms nothing, which is applied as a copy in Julia rather than handed to VkFFT (see VkFFTLayout).\n\nPlans are cached and reused, so two calls to VkFFT.plan_fft with the same array shape, region and device return the same object. VkFFT.clear_cache!() drops the cache.\n\nFields\n\napp::Ptr{Cvoid}: The equivalent of an FFTW plan but for VkFFT. C_NULL when there is nothing to transform\nsz::NTuple{N, Int}: The size of the input array\nregion::NTuple{M, Int}: The dimensions on which to perform the FFT, sorted\ndirection::Int32: -1 for forward, 1 for inverse\nnormalize::Bool: Whether VkFFT applies 1/N in-kernel, which is what makes this an ifft rather than a bfft plan\nzeropad::NTuple{2, Int}: The zero-padded range on the first transformed dimension, (0, 0) for none\ndevice_id::UInt64: Identity of the device context the plan was built for\nroots::Vector{Any}: Backend objects (context, queue, ...) the app outlives nothing without\nlock::ReentrantLock: Held for the length of one application, because a VkFFT app is not reentrant\ndestroyed::Bool: Set by the finalizer so a double destroy is a no-op\npinv::Union{Nothing, VkFFTPlan{T, N, IP, B, M}}: Cached raw inverse plan\n\n\n\n\n\n","title":"VkFFT.VkFFTPlan"},{"category":"type","location":"api/#VkFFT.VkFFTRealPlan","page":"API reference","text":"VkFFTRealPlan{T, S, N, B, M}\n\nA first-class AbstractFFTs.Plan for VkFFT's real-to-complex and complex-to-real transforms.\n\nThe type parameters are (T) the input element type, (S) the output element type, (N) ndims of both arrays, (B) the backend tag (:cuda, :opencl or :metal) and (M) the number of transformed dimensions. A forward plan pairs a real T with a complex S and an inverse plan the other way around, so mul! and * type check by dispatch alone. There is no in-place parameter because AbstractFFTs has no in-place real transform to implement.\n\nd is the real length of the halved dimension. The halved complex length does not recover it (d = 12 and d = 13 both give 7), which is why d is part of the plan and of the cache key.\n\nAn inverse plan is only defined on a spectrum that really is the transform of a real signal, which means one whose DC values, and its Nyquist values when d is even, are real. VkFFT packs two such spectra into one complex pass, so for any other input it returns something different from what FFTW returns.\n\nPlans are cached and reused, as VkFFT.plan_fft's are.\n\nFields\n\napp::Ptr{Cvoid}: The equivalent of an FFTW plan but for VkFFT. C_NULL when there is nothing to transform\nsz::NTuple{N, Int}: The size of the input array\nosz::NTuple{N, Int}: The size of the output array\nregion::NTuple{M, Int}: The dimensions on which to perform the FFT, sorted\nd::Int: The real length of the halved dimension\ndirection::Int32: -1 for forward, 1 for inverse\nnormalize::Bool: Whether VkFFT applies 1/N in-kernel, which is what makes this an irfft rather than a brfft plan\nzeropad::NTuple{2, Int}: The zero-padded range on the halved dimension, (0, 0) for none\ndevice_id::UInt64: Identity of the device context the plan was built for\nroots::Vector{Any}: Backend objects (context, queue, ...) the app outlives nothing without\nlock::ReentrantLock: Held for the length of one application, because a VkFFT app is not reentrant\ndestroyed::Bool: Set by the finalizer so a double destroy is a no-op\npinv::Union{Nothing, VkFFTRealPlan{S, T, N, B, M}}: Cached raw inverse plan\n\n\n\n\n\n","title":"VkFFT.VkFFTRealPlan"},{"category":"type","location":"api/#VkFFT.VkFFTR2RPlan","page":"API reference","text":"VkFFTR2RPlan{T, N, IP, B, M}\n\nA first-class AbstractFFTs.Plan for VkFFT's real-to-real transforms.\n\nThe type parameters are (T) the element type, (N) ndims of the arrays, (IP) whether the plan is in-place, (B) the backend tag (:cuda, :opencl or :metal) and (M) the number of transformed dimensions. Input and output have the same size and the same element type, so p * x gives an array shaped like x.\n\nkind and type name the transform: :dct with type 2 is FFTW's REDFT10, :dst with type 3 is RODFT01, and so on. VkFFT reads one DCT type and one DST type per plan and applies it to every transformed axis, so a plan cannot mix kinds or types the way FFTW's per-axis kinds tuple can.\n\nAn inverse plan carries the same kind and type as the forward plan it undoes, and computes that transform's inverse: the type-3 transform of the same kind for a type-2 plan, the type-2 for a type-3 plan, and the same type for types 1 and 4. VkFFT applies the exact per-type scale factor in-kernel, so an inverse plan is a true inverse and needs no scaling pass.\n\nPlans are cached and reused, as VkFFT.plan_fft's are.\n\nFields\n\napp::Ptr{Cvoid}: The equivalent of an FFTW plan but for VkFFT. C_NULL when there is nothing to transform\nsz::NTuple{N, Int}: The size of both arrays\nregion::NTuple{M, Int}: The dimensions on which to perform the transform, sorted\nkind::Symbol: :dct or :dst\ntype::Int: The transform type, 1 to 4\ndirection::Int32: -1 for forward, 1 for inverse\nnormalize::Bool: Whether VkFFT applies the per-type 1/S in-kernel\nzeropad::NTuple{2, Int}: The zero-padded range on the first transformed dimension, (0, 0) for none\ndevice_id::UInt64: Identity of the device context the plan was built for\nroots::Vector{Any}: Backend objects (context, queue, ...) the app outlives nothing without\nlock::ReentrantLock: Held for the length of one application, because a VkFFT app is not reentrant\ndestroyed::Bool: Set by the finalizer so a double destroy is a no-op\npinv::Union{Nothing, VkFFTR2RPlan{T, N, IP, B, M}}: Cached raw inverse plan\n\n\n\n\n\n","title":"VkFFT.VkFFTR2RPlan"},{"category":"type","location":"api/#VkFFT.VkFFTConvPlan","page":"API reference","text":"VkFFTConvPlan{T, N, B, M}\n\nA first-class AbstractFFTs.Plan for VkFFT's fused circular convolution.\n\nThe type parameters are (T) the element type, (N) ndims of the arrays, (B) the backend tag (:cuda, :opencl or :metal) and (M) the number of transformed dimensions. Input and output have the same size and element type, so p * x gives an array shaped like x. What p * x computes, what regions and kernels a plan takes and why it has neither an inverse nor an adjoint is VkFFT.plan_conv's docstring.\n\nPlans are not cached. A convolution app carries the kernel it was pointed at, so two plans of the same shape and different kernels must never share one, and the kernel lives on the device where a cache key cannot cheaply reach it.\n\nFields\n\napp::Ptr{Cvoid}: The equivalent of an FFTW plan but for VkFFT, running the whole fft/multiply/inverse pipeline\nsz::NTuple{N, Int}: The size of both arrays\nregion::NTuple{M, Int}: The dimensions on which to perform the convolution, sorted\nfeatures::Int: The trailing component stack, VkFFT's coordinateFeatures\ncorrelate::Bool: Whether the transformed input is conjugated, giving a cross-correlation\nkernel::Vector{Any}: The one device array holding the transformed kernel, which VkFFT reads at every dispatch\ndevice_id::UInt64: Identity of the device context the plan was built for\nroots::Vector{Any}: Backend objects (context, queue, ...) the app outlives nothing without\nlock::ReentrantLock: Held for the length of one application, because a VkFFT app is not reentrant\ndestroyed::Bool: Set by the finalizer so a double destroy is a no-op\n\n\n\n\n\n","title":"VkFFT.VkFFTConvPlan"},{"category":"type","location":"api/#VkFFT.VkFFTUnsafePlan","page":"API reference","text":"VkFFTUnsafePlan{T, S, N, M, B}\n\nA first-class AbstractFFTs.Plan built from a raw VkFFTConfig.\n\nThe type parameters are (T) the input element type, (S) the output element type, (N) ndims of the input, (M) ndims of the output and (B) the backend tag (:cuda, :opencl or :metal). All five come from the prototype arrays VkFFT.unsafe_plan was given, since a vkfft_config describes a byte layout and knows nothing about Julia shapes.\n\nNone of the guards the other plan families run were applied to the configuration behind this plan. VkFFT.unsafe_plan documents what that means.\n\nPlans of this kind are never cached: two calls with equal configurations build two VkFFT applications.\n\nFields\n\napp::Ptr{Cvoid}: The equivalent of an FFTW plan but for VkFFT\nsz::NTuple{N, Int}: The size of the input array\nosz::NTuple{M, Int}: The size of the output array\ndirection::Int32: -1 for forward, 1 for inverse\ninplace::Bool: Whether the configuration said one buffer, in which case an application needs y === x\ndevice_id::UInt64: Identity of the device context the plan was built for\nroots::Vector{Any}: Backend objects (context, queue, ...) the app outlives nothing without\nlock::ReentrantLock: Held for the length of one application, because a VkFFT app is not reentrant\ndestroyed::Bool: Set by the finalizer so a double destroy is a no-op\n\n\n\n\n\n","title":"VkFFT.VkFFTUnsafePlan"},{"category":"function","location":"api/#VkFFT.plan_fft","page":"API reference","text":"plan_fft(x, region=1:ndims(x); zeropad=nothing, tune=false)\n\nCreates an out-of-place forward complex-to-complex VkFFT plan for x.\n\nThe plan is an AbstractFFTs.Plan, so p * x, mul!(y, p, x), inv(p), p \\ y, p', size(p) and AbstractFFTs.fftdims(p) all work. It is not reachable through the global fft/plan_fft: those belong to whichever package owns the array type.\n\nx must be a contiguous device array of ComplexF16, ComplexF32 or ComplexF64 on a backend whose extension is loaded, and it is used only for its type, size and device. Its contents are neither read nor written to. ComplexF16 is a storage precision: VkFFT reads and writes 16-bit values and computes in fp32, so the accuracy comes from the 11-bit mantissa. It needs a device that supports half precision, which on OpenCL means one reporting cl_khr_fp16. On CUDA it needs more than the card: VkFFT compiles its half-precision kernels by including cuda_fp16.h from the CUDA toolkit path baked into libvkfft, so a wrapper built without one refuses half precision up front. Building the wrapper against a local CUDA toolkit bakes a real path.\n\nzeropad=lo:hi declares the samples lo:hi of the first transformed dimension zero. A forward plan never reads them, so they need not be zeroed: whatever is there does not reach the result. VkFFT only skips the block correctly on its own first axis, so the padded dimension has to be the first one of x longer than 1, and any other region is refused with advice to permute. A padded plan has neither an inverse nor an adjoint, since nothing can put back samples that were never read.\n\ntune=true picks VkFFT's coalesced_memory and aim_threads for this shape on this device, by building a small grid of candidate plans and timing warmed-up applications of each. The winner is stored on disk, so the same call in a later session and in a later process is a file read. The sweep allocates buffers of the plan's own shape to time on, and it takes as long as building and timing sixteen plans takes, which is the price of the first call only. tune=:force sweeps again and overwrites what is stored, for a machine whose driver has changed underneath its records, and VkFFT.clear_tuning!() drops them all. Tuning applies to the plan being built and not to the inverse plans inv derives from it.\n\nArguments\n\nx: The input array\nregion=1:ndims(x): The dimensions on which to perform the FFT\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTPlan\n\n\n\n\n\n","title":"VkFFT.plan_fft"},{"category":"function","location":"api/#VkFFT.plan_fft!","page":"API reference","text":"plan_fft!(x, region=1:ndims(x); zeropad=nothing, tune=false)\n\nCreates an in-place forward complex-to-complex VkFFT plan for x.\n\nLike VkFFT.plan_fft, except that p * x overwrites x and mul!(y, p, x) demands y === x.\n\nZero-padding and tune work as in VkFFT.plan_fft.\n\nArguments\n\nx: The input array\nregion=1:ndims(x): The dimensions on which to perform the FFT\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTPlan\n\n\n\n\n\n","title":"VkFFT.plan_fft!"},{"category":"function","location":"api/#VkFFT.plan_bfft","page":"API reference","text":"plan_bfft(x, region=1:ndims(x); zeropad=nothing, tune=false)\n\nCreates an out-of-place unnormalized inverse complex-to-complex VkFFT plan for x.\n\nUnnormalized: p * (VkFFT.plan_fft(x, region) * x) is prod(size(x)[region]) times x. Use VkFFT.plan_ifft for the normalized inverse, which is cheaper than scaling afterwards.\n\nzeropad=lo:hi declares the samples lo:hi of the first transformed dimension zero, as in VkFFT.plan_fft, except that an inverse plan skips writing that range instead of skipping reads. It is left holding whatever the destination already held, so it is undefined in the array p * x allocates, and the skip is not reliable above one dimension. Treat that range as garbage, never as zeros. tune works as in VkFFT.plan_fft.\n\nArguments\n\nx: The input array\nregion=1:ndims(x): The dimensions on which to perform the FFT\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTPlan\n\n\n\n\n\n","title":"VkFFT.plan_bfft"},{"category":"function","location":"api/#VkFFT.plan_bfft!","page":"API reference","text":"plan_bfft!(x, region=1:ndims(x); zeropad=nothing, tune=false)\n\nCreates an in-place unnormalized inverse complex-to-complex VkFFT plan for x.\n\nZero-padding and tune work as in VkFFT.plan_bfft.\n\nArguments\n\nx: The input array\nregion=1:ndims(x): The dimensions on which to perform the FFT\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTPlan\n\n\n\n\n\n","title":"VkFFT.plan_bfft!"},{"category":"function","location":"api/#VkFFT.plan_ifft","page":"API reference","text":"plan_ifft(x, region=1:ndims(x); zeropad=nothing, tune=false)\n\nCreates an out-of-place normalized inverse complex-to-complex VkFFT plan for x.\n\nThe 1/N is applied by VkFFT inside the transform kernels, so this costs one kernel launch, not two. N is the product of the transformed axis lengths only: batched and omitted dimensions never enter it.\n\nZero-padding and tune work as in VkFFT.plan_bfft, the padded range of an inverse plan being skipped writes rather than skipped reads.\n\nArguments\n\nx: The input array\nregion=1:ndims(x): The dimensions on which to perform the FFT\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTPlan\n\n\n\n\n\n","title":"VkFFT.plan_ifft"},{"category":"function","location":"api/#VkFFT.plan_ifft!","page":"API reference","text":"plan_ifft!(x, region=1:ndims(x); zeropad=nothing, tune=false)\n\nCreates an in-place normalized inverse complex-to-complex VkFFT plan for x.\n\nZero-padding and tune work as in VkFFT.plan_ifft.\n\nArguments\n\nx: The input array\nregion=1:ndims(x): The dimensions on which to perform the FFT\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTPlan\n\n\n\n\n\n","title":"VkFFT.plan_ifft!"},{"category":"function","location":"api/#VkFFT.plan_rfft","page":"API reference","text":"plan_rfft(x, region=1:ndims(x); zeropad=nothing, tune=false)\n\nCreates a forward real-to-complex VkFFT plan for x.\n\nThe plan is an AbstractFFTs.Plan, so p * x, mul!(y, p, x), inv(p), p \\ y, p', size(p) and AbstractFFTs.fftdims(p) all work. It is not reachable through the global rfft/plan_rfft: those belong to whichever package owns the array type.\n\nThe first transformed dimension is halved, so the output has size(x, d1) ÷ 2 + 1 along it and size(x) everywhere else. VkFFT can only halve its own first axis, which means that dimension has to be the first one of x longer than 1. Any other region is refused with advice to permute. There is no in-place form, because AbstractFFTs has no in-place real transform.\n\nx must be a contiguous device array of Float16, Float32 or Float64, used as in VkFFT.plan_fft: only its type, size and device matter, and Float16 is a storage precision carrying the device and wrapper requirements VkFFT.plan_fft lists.\n\nZero-padding and tune work as in VkFFT.plan_fft. The padded range lies on the halved dimension, which is the one axis VkFFT can pad and the axis it already has to be.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the FFT\nzeropad=nothing: A 1-based inclusive range on the halved dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTRealPlan\n\n\n\n\n\n","title":"VkFFT.plan_rfft"},{"category":"function","location":"api/#VkFFT.plan_brfft","page":"API reference","text":"plan_brfft(x, d, region=1:ndims(x); tune=false)\n\nCreates an unnormalized inverse complex-to-real VkFFT plan for the spectrum x.\n\nd is the length of the real signal along the halved dimension, which the spectrum does not carry: d = 12 and d = 13 both leave 7 complex values. Unnormalized: p * (VkFFT.plan_rfft(r, region) * r) is prod(size(r)[region]) times r. Use VkFFT.plan_irfft for the normalized inverse, which is cheaper than scaling afterwards.\n\nApplying the plan clobbers x, which VkFFT uses as scratch space whenever more than one axis is transformed. FFTW's brfft destroys its input too. Copy the spectrum first if you still need it.\n\ntune works as in VkFFT.plan_fft.\n\nArguments\n\nx: The complex input spectrum\nd: The length of the real signal along the halved dimension\nregion=1:ndims(x): The dimensions on which to perform the FFT\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTRealPlan\n\n\n\n\n\n","title":"VkFFT.plan_brfft"},{"category":"function","location":"api/#VkFFT.plan_irfft","page":"API reference","text":"plan_irfft(x, d, region=1:ndims(x); tune=false)\n\nCreates a normalized inverse complex-to-real VkFFT plan for the spectrum x.\n\nThe 1/N is applied by VkFFT inside the transform kernels, so this costs one kernel launch, not two. N is the product of the transformed axis lengths of the real signal, d included, and never of the batched or omitted ones.\n\nApplying the plan clobbers x, exactly as VkFFT.plan_brfft does, and tune works as in VkFFT.plan_fft.\n\nArguments\n\nx: The complex input spectrum\nd: The length of the real signal along the halved dimension\nregion=1:ndims(x): The dimensions on which to perform the FFT\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTRealPlan\n\n\n\n\n\n","title":"VkFFT.plan_irfft"},{"category":"function","location":"api/#VkFFT.plan_dct","page":"API reference","text":"plan_dct(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an out-of-place forward discrete cosine transform VkFFT plan for x.\n\nThe plan is an AbstractFFTs.Plan, so p * x, mul!(y, p, x), inv(p), p \\ y, p', size(p) and AbstractFFTs.fftdims(p) all work. Output and input have the same size and element type.\n\nThe transform is FFTW's, element for element and at every size: type 1 to 4 are REDFT00, REDFT10, REDFT01 and REDFT11, unnormalized, so a round trip through VkFFT.plan_idct of the same type comes back at a scale of exactly one. VkFFT reads one type per plan and applies it to every transformed axis, so FFTW's per-axis kinds tuple has no equivalent here.\n\nEvery transformed dimension has to have length at least 2. VkFFT drops a length-1 axis, which computes the identity along it where FFTW scales, and it hangs while planning a type-1 transform of one, an upstream VkFFT bug.\n\nx must be a contiguous device array of Float16, Float32 or Float64, used as in VkFFT.plan_fft: only its type, size and device matter, and Float16 is a storage precision carrying the device and wrapper requirements VkFFT.plan_fft lists. Zero-padding and tune work as in VkFFT.plan_fft.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DCT type, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_dct"},{"category":"function","location":"api/#VkFFT.plan_dct!","page":"API reference","text":"plan_dct!(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an in-place forward discrete cosine transform VkFFT plan for x.\n\nLike VkFFT.plan_dct, except that p * x overwrites x and mul!(y, p, x) demands y === x.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DCT type, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_dct!"},{"category":"function","location":"api/#VkFFT.plan_idct","page":"API reference","text":"plan_idct(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an out-of-place inverse discrete cosine transform VkFFT plan for x.\n\ntype names the forward transform this undoes, not the transform this computes: a type=2 plan applies REDFT01 divided by prod(2 .* size(x)[region]), which is what inverts REDFT10. Types 1 and 4 invert to their own kind, over prod(2 .* size(x)[region] .- 2) for type 1. VkFFT applies the factor inside the transform kernels, so this costs one kernel launch rather than a transform plus a scaling pass, and the round trip is exact rather than off by a scale.\n\nZero-padding and tune work as in VkFFT.plan_bfft, the padded range of an inverse plan being skipped writes rather than skipped reads.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DCT type this inverts, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_idct"},{"category":"function","location":"api/#VkFFT.plan_idct!","page":"API reference","text":"plan_idct!(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an in-place inverse discrete cosine transform VkFFT plan for x.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DCT type this inverts, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_idct!"},{"category":"function","location":"api/#VkFFT.plan_dst","page":"API reference","text":"plan_dst(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an out-of-place forward discrete sine transform VkFFT plan for x.\n\nFFTW's RODFT00, RODFT10, RODFT01 and RODFT11 for type 1 to 4, unnormalized, element for element at every size. VkFFT reformulates the sine transforms as cosine transforms internally, and the result is FFTW's with no sign or index divergence anywhere. Everything else matches VkFFT.plan_dct.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DST type, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_dst"},{"category":"function","location":"api/#VkFFT.plan_dst!","page":"API reference","text":"plan_dst!(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an in-place forward discrete sine transform VkFFT plan for x.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DST type, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_dst!"},{"category":"function","location":"api/#VkFFT.plan_idst","page":"API reference","text":"plan_idst(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an out-of-place inverse discrete sine transform VkFFT plan for x.\n\ntype names the forward transform this undoes, exactly as in VkFFT.plan_idct. The type-1 factor is prod(2 .* size(x)[region] .+ 2) here, which is FFTW's for RODFT00.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DST type this inverts, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_idst"},{"category":"function","location":"api/#VkFFT.plan_idst!","page":"API reference","text":"plan_idst!(x, region=1:ndims(x); type=2, zeropad=nothing, tune=false)\n\nCreates an in-place inverse discrete sine transform VkFFT plan for x.\n\nArguments\n\nx: The real input array\nregion=1:ndims(x): The dimensions on which to perform the transform\ntype=2: The DST type this inverts, 1 to 4\nzeropad=nothing: A 1-based inclusive range on the first transformed dimension VkFFT may treat as zero\ntune=false: Whether to tune VkFFT's block and thread knobs for this shape, :force to re-sweep\n\nReturns\n\nA VkFFTR2RPlan\n\n\n\n\n\n","title":"VkFFT.plan_idst!"},{"category":"function","location":"api/#VkFFT.plan_conv","page":"API reference","text":"plan_conv(x, kernel, region=1:ndims(x); correlate=false, zeropad=nothing)\n\nCreates a fused circular convolution VkFFT plan for x against kernel.\n\nThe plan is an AbstractFFTs.Plan, so p * x, mul!(y, p, x), size(p) and AbstractFFTs.fftdims(p) work. Output and input have the same size and element type. VkFFT runs the forward transform, the multiply against the transformed kernel and the inverse transform as one application, and applies the whole 1/N itself, so p * x is the plain circular convolution at a scale of exactly one. Along one transformed axis of length n, (p * x)[m] == sum(x[j] * kernel[mod(m - j, n) + 1] for j in 1:n), and the transform is separable over the transformed axes.\n\ncorrelate=true conjugates the transformed input instead, which makes the result the circular cross-correlation (p * x)[m] == sum(conj(x[j]) * kernel[mod(j + m - 2, n) + 1] for j in 1:n). Conjugating the kernel rather than the input is a documented VkFFT option that no version vendored here generates code for, so it is not offered.\n\nkernel is read once, here. The plan transforms it into a device buffer of its own and holds on to that buffer for as long as the plan lives, because VkFFT reads it at every application and copies nothing. Writing to kernel afterwards, or freeing it, does not affect the plan.\n\nx and kernel must be contiguous device arrays of ComplexF32 or ComplexF64 of the same size, on the same device, on a backend whose extension is loaded. x is used only for its type, size and device. ComplexF16 is refused here even though the other families accept it: VkFFT builds a half-precision convolution plan, reports success and returns something that is no convolution.\n\nThe transformed dimensions have to run from the first dimension of x longer than 1 up to a trailing run of untransformed ones, since VkFFT cannot convolve over a layout that omits an axis, and there can be at most two of them, since VkFFT computes something that is no convolution over three. Any other region is refused rather than passed on. That trailing run is a stack of independent components, each convolved against its own slice of the kernel, so one kernel meant for all of them has to be broadcast into kernel by the caller.\n\nOne family of transformed lengths comes back wrong and cannot be refused from here. VkFFT reaches for Bluestein's algorithm whenever an axis length has a prime factor larger than 13, and a Bluestein axis long enough that VkFFT splits it over several uploads computes something that is no convolution, an upstream VkFFT bug: measured on pocl in Float64, lengths 4093, 8191 and 16381 return a result with no resemblance to the answer and no error code, while every length whose prime factors are all at most 13 is correct up to 16384 and beyond, and shorter Bluestein lengths such as 1021 and 727 are correct too. The length at which VkFFT starts splitting depends on the device's shared memory, so the planner has nothing to test against, which is why this is a warning and not a refusal. Check one convolution against a host reference before trusting a transformed length with a large prime factor.\n\nThere is no inverse and no adjoint. A convolution against a kernel with a zero in its spectrum loses information, so it has no inverse in general, and the adjoint would have to be written around the normalization VkFFT applies inside the pipeline. Both refuse rather than return something plausible.\n\nConvolution plans are not cached, so two calls with the same shapes give two plans with two applications. A convolution app carries the kernel it was pointed at, which is what makes it unshareable.\n\nThere is no tune. A convolution plan is a forward transform, a multiply and an inverse in one app, and the two knobs the tuner sweeps set the block and thread shape of the transforms, so tuning the plain VkFFT.plan_fft of the same axes answers the same question at a third of the sweep. Tuning the pipeline directly is worth adding once there is a measurement saying the multiply moves the optimum.\n\nArguments\n\nx: The prototype input array\nkernel: The convolution kernel, the same size as x, read at plan time\nregion=1:ndims(x): The dimensions on which to perform the convolution\ncorrelate=false: Whether to compute the circular cross-correlation instead\nzeropad=nothing: Not supported yet, and refused with an error rather than ignored\n\nReturns\n\nA VkFFTConvPlan\n\n\n\n\n\n","title":"VkFFT.plan_conv"},{"category":"type","location":"api/#VkFFT.VkFFTConfig","page":"API reference","text":"VkFFTConfig\n\nJulia mirror of vkfft_config, the whole of what the wrapper exposes of VkFFT's configuration.\n\nField order and types are part of the wrapper's ABI. Zero means \"VkFFT's default\" for every field, so a zeroed struct plus fft_dim and size is a valid complex-to-complex plan. Build one with the keyword constructor, which leaves every field VkFFT defaults to zero at zero, and hand it to VkFFT.unsafe_plan to get a plan out of it. Nothing validates the combination: read VkFFT.unsafe_plan's docstring for what that costs.\n\nAxes run fastest first, which is Julia's column-major order, and size slots past fft_dim must be zero. precision 3 selects VkFFT's double-double quad arithmetic, which stores four native floats per complex value and so has no Julia element type of its own.\n\nFields\n\nfft_dim::UInt64: Number of VkFFT axes, i.e. how many size slots are meaningful\nsize::NTuple{12, UInt64}: Axis lengths, fastest axis first. Unused slots must be 0\nomit::NTuple{12, UInt64}: 1 = no FFT along this axis, but keep it in the layout\nnumber_batches::UInt64: Extra outermost batch count. 0 leaves VkFFT's default of 1\nprecision::Cint: 0 = fp32 (Float32), 1 = fp64 (Float64), 2 = fp16 (Float16), 3 = quad (Float128)\nr2c::Cint: 1 = R2C forward / C2R inverse\ndct::Cint: DCT type 1..4. 0 = off\ndst::Cint: DST type 1..4. 0 = off\ninplace::Cint: 1 = one buffer. 0 = read in, write out\nnormalize::Cint: 1 = apply 1/N in-kernel on the inverse\nmake_forward_only::Cint: Skip inverse kernel generation\nmake_inverse_only::Cint: Skip forward kernel generation\nzeropad_left::NTuple{12, UInt64}: VkFFT fft_zeropad_left\nzeropad_right::NTuple{12, UInt64}: VkFFT fft_zeropad_right\nperform_zeropad::NTuple{12, Cint}: VkFFT performZeropadding, per axis\ncoalesced_memory::UInt64: Tuning, in bytes (0 = auto)\naim_threads::UInt64: Tuning, threads per block (0 = auto)\nnum_shared_banks::UInt64: Tuning (0 = auto)\ncoordinate_features::UInt64: VkFFT coordinateFeatures. 0 leaves VkFFT's default of 1\nnumber_kernels::UInt64: VkFFT numberKernels. 0 leaves VkFFT's default of 1\nperform_convolution::Cint: 1 = fft, multiply, inverse in one plan\nkernel_convolution::Cint: 1 = this plan only transforms a convolution kernel\nconjugate_convolution::Cint: 1 = conjugate the transformed input, giving a cross-correlation\nsave_to_string::Cint: 1 = keep the compiled binaries for vkfft_save\n\n\n\n\n\n","title":"VkFFT.VkFFTConfig"},{"category":"method","location":"api/#VkFFT.VkFFTConfig-Tuple{}","page":"API reference","text":"VkFFTConfig(; fft_dim, size, omit, number_batches=0, precision=0, ...)\n\nBuilds a VkFFTConfig with every field that VkFFT defaults to zero left at zero.\n\nfft_dim and size are the two required keywords, and a config with nothing else set is a forward and inverse out-of-place complex-to-complex plan in fp32. The keywords mirror the struct's fields one for one, so the # Fields list of VkFFTConfig is what each of them means.\n\nReturns\n\nA VkFFTConfig\n\n\n\n\n\n","title":"VkFFT.VkFFTConfig"},{"category":"function","location":"api/#VkFFT.unsafe_plan","page":"API reference","text":"unsafe_plan(config::VkFFTConfig, in_prototype, out_prototype; direction=-1)\n\nCreates a plan from a VkFFTConfig verbatim, with none of the planner's guards in the way.\n\nThe two prototype arrays supply what a vkfft_config cannot: the backend, the device context, the element types and the Julia array shapes an application is checked against. Their contents are neither read nor written, and a configuration with inplace = 1 takes one array as both prototypes. Everything about the transform itself comes from config as written, which is the point of this entry point and the reason for its name.\n\nWhat unsafe_ means here is specific. The other entry points refuse a list of configurations VkFFT accepts and then does something other than what was asked, and not one of those refusals runs on this path:\n\nA configuration with fft_dim = 0, or one whose every axis is omitted, builds and reports success and dispatches no kernel, so an out-of-place caller is left holding whatever the output buffer already held.\nA type-1 DCT or DST of an axis of length 1 sets that axis' internal transform length to zero and VkFFT's radix decomposition never terminates. Nothing reports anything: the call into vkfft_create does not return.\nA dct and a dst set at once computes the DST, or at other type pairs something that is neither a DCT nor a DST while still round-tripping. An r2c plan carrying either corrupts the spectrum. A plan setting both perform_convolution and kernel_convolution quietly ignores the second. A perform_convolution plan built with make_forward_only or make_inverse_only leaves VkFFT dereferencing the half it was told to skip.\nA dct or dst outside 1 to 4 falls through every branch that generates the pre- and post-processing an r2r transform needs, leaving a plain real transform behind.\nperform_zeropad is only skipped correctly on VkFFT's own first axis. On any other axis the plan runs and reads uninitialized memory.\nHalf precision on a device without it comes back as a failed compile rather than as anything a message can explain, and so does half precision on CUDA through a wrapper with no baked toolkit path. A half-precision convolution reports success and returns something that is no convolution.\n\nThe damage is not confined to the result. The transform VkFFT generates for a configuration like these is not the transform the buffers were sized for, so applying one writes past the end of them and corrupts the process heap. The crash then lands wherever that heap is next touched, which has been inside pocl's program teardown and inside Julia's own compiler. Work out the byte layout your configuration implies before applying it.\n\nPrecision 3, VkFFT's double-double quad, is reachable only from here. No Julia element type has its four-native-floats-per-value layout, so the prototypes stand in for it by byte count and the packing and unpacking are the caller's.\n\nThe plan applies with mul!(y, p, x) and p * x, and it is freed by its finalizer or by VkFFT.unsafe_free! like every other plan. It has no inverse, no adjoint and no AbstractFFTs.fftdims: nothing here knows which Julia dimensions config.size was built from. size(p) is the input prototype's size and AbstractFFTs.output_size(p) the output prototype's.\n\nArguments\n\nconfig::VkFFTConfig: The configuration, forwarded to the wrapper unchanged\nin_prototype: A device array carrying the input's element type, size and device\nout_prototype: A device array carrying the output's element type and size, on the same device\ndirection=-1: Which direction to run, in VkFFTAppend's encoding of -1 for forward and 1 for inverse\n\nReturns\n\nA VkFFTUnsafePlan\n\n\n\n\n\n","title":"VkFFT.unsafe_plan"},{"category":"function","location":"api/#VkFFT.unsafe_free!","page":"API reference","text":"unsafe_free!(plan::AbstractVkFFTPlan)\n\nFrees the plan's VkFFT application now instead of waiting for the garbage collector, at most once.\n\nUnsafe because nothing stops another reference to the same plan from being applied afterwards, and because a cached plan stays in the cache. Call VkFFT.clear_cache!() first if you mean to free everything.\n\nThis is also every plan's finalizer, so it runs from the garbage collector: a flag write and one ccall, wrapped in whatever context guard the backend needs (_with_plan_context). The backend objects behind roots are released by their own finalizers afterwards, in whatever order the collector picks, which is why the guard has to tolerate a context that is already gone.\n\nReturns\n\nnothing\n\n\n\n\n\n","title":"VkFFT.unsafe_free!"},{"category":"function","location":"api/#VkFFT.clear_cache!","page":"API reference","text":"clear_cache!()\n\nEmpties the plan cache, so the plans in it can be finalized.\n\nReturns\n\nnothing\n\n\n\n\n\n","title":"VkFFT.clear_cache!"},{"category":"function","location":"api/#VkFFT.cache_size","page":"API reference","text":"cache_size()\n\nReturns the number of plans currently held by the plan cache.\n\n\n\n\n\n","title":"VkFFT.cache_size"},{"category":"function","location":"api/#VkFFT.clear_tuning!","page":"API reference","text":"clear_tuning!()\n\nDeletes every stored tuning record.\n\nThe next tune=true on each shape sweeps again. tune=:force does the same for one shape without touching the rest.\n\nReturns\n\nThe number of files removed\n\n\n\n\n\n","title":"VkFFT.clear_tuning!"},{"category":"function","location":"api/#VkFFT.last_sweep","page":"API reference","text":"last_sweep()\n\nReturns the grid the most recent autotuner sweep measured, fastest first.\n\nEach entry is (coalesced_memory, aim_threads, microseconds), the last being the time one application took. Empty until a sweep has run in this session, and overwritten by the next one, so two tasks tuning at once leave only one of the two grids behind.\n\n\n\n\n\n","title":"VkFFT.last_sweep"},{"category":"function","location":"api/#VkFFT.sweep_count","page":"API reference","text":"sweep_count()\n\nReturns how many autotuner sweeps this session has run.\n\nA tune=true that finds a stored record does not sweep, so this counts the expensive calls only. It exists to make \"the second call was a lookup\" a thing a test can assert rather than infer from a stopwatch.\n\n\n\n\n\n","title":"VkFFT.sweep_count"},{"category":"function","location":"api/#VkFFT.disk_cache_dir","page":"API reference","text":"disk_cache_dir()\n\nReturns the directory the tuning records live in.\n\nA Scratch.jl space, so it survives across sessions and is garbage collected with the package rather than with the depot. The filenames carry a backend name and the first twelve hex digits of the key, which is enough to tell at a glance what a directory listing is holding.\n\nReturns\n\nThe path of the scratch space, created if it did not exist\n\n\n\n\n\n","title":"VkFFT.disk_cache_dir"},{"category":"type","location":"api/#VkFFT.VkFFTError","page":"API reference","text":"VkFFTError(code::Int, name::String)\n\nAn error reported by the VkFFT C library.\n\nFields\n\ncode::Int: The raw VkFFTResult code, as returned by the wrapper\nname::String: The name of the code, from VkFFT's own getVkFFTErrorString\n\n\n\n\n\n","title":"VkFFT.VkFFTError"},{"category":"section","location":"transforms/#Transforms","page":"Transforms","text":"The examples below use CLArray, but CuArray and MtlArray work the same way.\n\nThe first argument to every entry point is a dummy array. Only its element type, its size and its device are used. Its contents are never read nor written, so you can plan from one buffer and apply the plan to another of the same shape.","title":"Transforms"},{"category":"section","location":"transforms/#Regions","page":"Transforms","text":"region is any iterable of the dimensions to transform, as in AbstractFFTs, and it defaults to every dimension. Order does not matter. Repeats and out-of-bounds dimensions are refused and throw an error.\n\nv = rand(ComplexF32, 8, 128, 6) # 3 dimensions to work with\nw = CLArray{ComplexF32}(undef, size(v))\ncopyto!(w, v)\n\nVkFFT.plan_fft(w, 2)      # transform over dimension 2, batch over 1 and 3\nVkFFT.plan_fft(w, (1, 3)) # transform over dimension 1 and 3, do nothing to 2\nVkFFT.plan_fft(w, 1:2)    # any iterable works\nVkFFT.plan_fft(w, ())     # transforms nothing, and applies as a copy\n\nA region that transforms nothing, or one where every dimension has length 1, is applied as a copy in Julia.\n\nLength-1 dimensions are ignored. See Twelve axes for the cap on how many axes a region can need.","title":"Regions"},{"category":"section","location":"transforms/#Complex-to-complex","page":"Transforms","text":"VkFFT.plan_fft, VkFFT.plan_bfft and VkFFT.plan_ifft, each with an in-place ! form. Input and output have the same size and the same element type, one of ComplexF16, ComplexF32 and ComplexF64.\n\nh = rand(ComplexF32, 1024, 64)\nx = CLArray{ComplexF32}(undef, size(h))\ncopyto!(x, h)\n\nforward = VkFFT.plan_fft(x, 1) # out-of-place, dimension 1, batched over 2\ny = forward * x                # fft(h, 1)\nmul!(y, forward, x)            # same thing as above, but in-place into y\n\nunnormalized = VkFFT.plan_bfft(x, 1) # bfft(h, 1), no 1/N\nnormalized = VkFFT.plan_ifft(x, 1)   # ifft(h, 1), 1/N applied in-kernel\n\nback = inv(forward) * y # h again\n\ninplace = VkFFT.plan_fft!(x, 1) # mutating\ninplace * x # x now holds fft(h, 1)\n\nVkFFT.plan_ifft is a normalized inverse of its own and not a scaled bfft. VkFFT applies the 1/N inside the transform kernels, so an ifft is one kernel launch instead of two. The N is the product of the transformed axis lengths only, and batched and omitted dimensions never enter it.\n\nThe in-place forms overwrite their argument: p * x returns x itself, and mul!(y, p, x) requires that y === x.","title":"Complex to complex"},{"category":"section","location":"transforms/#Real-to-complex,-and-back","page":"Transforms","text":"VkFFT.plan_rfft forward, VkFFT.plan_brfft and VkFFT.plan_irfft back. The real side is one of Float16, Float32 and Float64 and the output has the matching complex type. There is no in-place form, because AbstractFFTs has no in-place real transform.\n\nThe first transformed dimension is halved, so the output has size(x, d1) ÷ 2 + 1 along it and size(x) everywhere else. That dimension has to be the first one of the array longer than 1, and using any other region throws an error.\n\nh = rand(Float32, 1024, 64)\nr = CLArray{Float32}(undef, size(h))\ncopyto!(r, h)\n\nforward = VkFFT.plan_rfft(r, 1) # 1024 reals in, 513 complex values out\nspectrum = forward * r\n\nnormalized = VkFFT.plan_irfft(spectrum, 1024, 1)   # applies to h\nunnormalized = VkFFT.plan_brfft(spectrum, 1024, 1) # applies to 1024 .* h\n\nsignal = normalized * spectrum # h again, and spectrum is now garbage\n\nBoth inverses need the real length, because the spectrum does not know which inverse to take. 513 complex values could have come from 1024 reals or from\n\nYou need to tell VkFFT which one to use.\n\nApplying an inverse consumes its input, which VkFFT uses as scratch whenever more than one axis is transformed. Copy the spectrum first if you still need it. FFTW's brfft destroys its input too.\n\nAn inverse is only defined on a spectrum that really is the transform of a real signal, meaning one whose DC values, and its Nyquist values when the real length is even, are real. VkFFT packs two such spectra into one complex pass, so for any other input it returns something different from what FFTW returns.","title":"Real to complex, and back"},{"category":"section","location":"transforms/#Real-to-real","page":"Transforms","text":"VkFFT.plan_dct and VkFFT.plan_dst forward, VkFFT.plan_idct and VkFFT.plan_idst back, each with an in-place ! form. Input and output have the same size and the same real element type.\n\ntype is 1 to 4 and defaults to 2. It selects FFTW's REDFT00, REDFT10, REDFT01 and REDFT11 for the cosine transforms and the matching RODFT kinds for the sine ones. The transform is FFTW's element for element at every size, prime lengths included.\n\nh = rand(Float32, 1024, 64)\nr = CLArray{Float32}(undef, size(h))\ncopyto!(r, h)\n\ncosine = VkFFT.plan_dct(r, 1) # REDFT10, unnormalized, batched over 2\ny = cosine * r\nback = inv(cosine) * y        # exactly h, the factor is applied in-kernel\n\nVkFFT.plan_dst(r, 1; type=4)  # RODFT11\nVkFFT.plan_idct(r, 1; type=3) # inverts a type-3 DCT\nVkFFT.plan_dct!(r, 1)         # in-place\n\nVkFFT.plan_idct and VkFFT.plan_idst take the type of the forward transform they undo, not the type they compute. An inverse of a type-2 DCT applies REDFT01 over prod(2 .* size(r)[region]). Types 1 and 4 invert to their own kind, over prod(2 .* size(r)[region] .- 2) for a type-1 DCT and prod(2 .* size(r)[region] .+ 2) for a type-1 DST. VkFFT applies the factor in-kernel, so a round trip lands on a scale of one and no ScaledPlan ever appears in this family.\n\nTwo restrictions have no equivalent in FFTW. One kind and one type apply to every transformed axis, so FFTW's per-axis kinds tuple cannot be expressed here. And every transformed dimension has to have length at least 2, since VkFFT computes the identity along a length-1 axis where FFTW scales, and it hangs while planning a type-1 transform of one.","title":"Real to real"},{"category":"section","location":"transforms/#Zero-padding","page":"Transforms","text":"zeropad=lo:hi declares the samples lo:hi of the first transformed dimension zero. It is available on the six complex-to-complex entry points, on VkFFT.plan_rfft and on all eight real-to-real ones.\n\np = VkFFT.plan_fft(x, 1; zeropad=513:1024) # only the first 512 rows matter\np * x                                      # the transform of the zeroed signal\n\nA forward plan never reads that range, so you do not have to clear it. VkFFT does not touch that range.\n\nAn inverse plan skips writing the range instead, and leaves it holding whatever the destination already held. You should treat that range as undefined, not as zeros, and note that the skip is not reliable above one dimension.\n\nThe padded dimension has to be the first transformed one, and that one has to be the first dimension of the array longer than 1. Any other region throws an error. The range itself is checked here too, because VkFFT clamps or ignores whatever it cannot use instead of rejecting it. hi may equal the axis length, which is how you cover the tail.\n\nA padded plan has neither an inverse nor an adjoint.","title":"Zero-padding"},{"category":"section","location":"transforms/#Fused-convolution","page":"Transforms","text":"VkFFT.plan_conv runs the forward transform, the multiply against a pre-transformed kernel and the inverse transform as one VkFFT application. Element types are ComplexF32 and ComplexF64.\n\nh = rand(ComplexF32, 256, 256)\nk = rand(ComplexF32, 256, 256)\nx = CLArray{ComplexF32}(undef, size(h))\nkernel = CLArray{ComplexF32}(undef, size(k))\ncopyto!(x, h)\ncopyto!(kernel, k)\n\np = VkFFT.plan_conv(x, kernel)                 # circular convolution\ny = p * x                                      # at a scale of exactly one\n\nc = VkFFT.plan_conv(x, kernel; correlate=true) # cross-correlation instead\n\nVkFFT applies the whole 1/N itself, so p * x is the plain circular convolution. Along one transformed axis of length n, (p * x)[m] == sum(x[j] * kernel[mod(m - j, n) + 1] for j in 1:n), and the transform is separable over the transformed axes. correlate=true conjugates the transformed input, which gives (p * x)[m] == sum(conj(x[j]) * kernel[mod(j + m - 2, n) + 1] for j in 1:n).\n\nkernel has to be the same element type, the same size and on the same device as x, and it is read once, here. The plan transforms it into a buffer of its own, so writing to your kernel array afterwards, or freeing it, does not affect the plan.\n\nAt most two dimensions can be transformed, and they have to run from the first dimension longer than 1 up to a trailing run of untransformed ones. Any other region is refused. That trailing run is a stack of independent components, each convolved against its own slice of the kernel, so broadcast the kernel yourself if one kernel is meant for all of them.\n\nWhat this family does not have:\n\n \nin-place form none\ninv and ' none\nzeropad refused with an error, not ignored\ntune none, tune the plain VkFFT.plan_fft of the same axes instead\nplan cache convolution plans are never cached\nComplexF16 refused, VkFFT returns something that is no convolution\n\nOne family of transformed lengths comes back wrong with nothing reported. See Known failures.","title":"Fused convolution"},{"category":"section","location":"tuning/#Tuning","page":"Tuning","text":"VkFFT takes two hints about how to decompose a transform into blocks and threads: coalesced_memory and aim_threads. coalesced_memory is a byte width the memory system is assumed to reward and aim_threads is a thread count per block. The optimal values depend on the device and the shape of the transform. Specifying tune=true finds the best values by measuring the time taken by many transforms.\n\np = VkFFT.plan_fft(x, (1, 2); tune=true)   # sweep, or read a stored answer\np = VkFFT.plan_fft(x, (1, 2); tune=:force) # sweep again and overwrite it\n\nVkFFT.last_sweep()     # what the last sweep measured, fastest first\nVkFFT.sweep_count()    # how many sweeps this session actually ran\nVkFFT.disk_cache_dir() # where the records live\nVkFFT.clear_tuning!()  # delete every record, and return how many\n\nVkFFT.last_sweep returns (coalesced_memory, aim_threads, microseconds) entries, fastest first. It is empty until a sweep has run in this session, and the next sweep overwrites it.\n\nfor (coalesced_memory, aim_threads, microseconds) in VkFFT.last_sweep()\n    println(\"$coalesced_memory bytes, $aim_threads threads: $microseconds us\")\nend\n\nVkFFT.sweep_count counts the expensive calls only. A tune=true that finds a stored record and skips the grid does not move it.\n\ntune works on the complex-to-complex, real-to-complex and real-to-real families. It is not available on VkFFT.plan_conv, and it applies to the plan being built rather than to the inverse plans inv derives from it. On a region that transforms nothing it plans the copy, sweeps nothing and writes no record.","title":"Tuning"},{"category":"section","location":"tuning/#What-the-sweep-measures","page":"Tuning","text":"The grid is 16 candidates, coalescedMemory over 0, 32, 64 and 128 bytes crossed with aimThreads over 0, 64, 128 and 256. A 0 leaves the choice to VkFFT, whose own defaults are 128 threads everywhere and:\n\ndevice coalescedMemory\nCUDA, NVIDIA and AMD OpenCL 32 bytes\nMetal, Intel, unrecognized OpenCL vendors 64 bytes\nany of the above, half precision doubled\n\nEach candidate is a real plan of the shape being tuned. Timing runs two warmed-up applications, then sizes a repeat count so that one candidate's run takes roughly 20 milliseconds, clamped to between 3 and 500 repeats and computed once so every candidate is compared over the same amount of work. mul! synchronizes before it returns, so the host clock measures device time plus one synchronization per application. The whole grid is timed twice and each candidate keeps its faster pass.\n\nThe sweep allocates buffers of the plan's own shape and fills them with ones. It does not touch the array you passed.\n\nWhen the sweep finishes, the winner stays in the plan cache and every loser is removed from it and freed, so the plan you get back is a cache hit and not a seventeenth compilation.\n\nThe cost of tuning is one sweep, sixteen plans built and timed, on the first call on a shape in the first process.","title":"What the sweep measures"},{"category":"section","location":"tuning/#Records-on-disk","page":"Tuning","text":"The outcomes of the tuning go into a Scratch.jl space, which survives across sessions and is garbage collected with the package rather than with the depot. Use VkFFT.disk_cache_dir to see where this scratch space is. A record is one file holding a comment line and two integers, named for the backend and the first twelve hex digits of its key.\n\nThe key covers the backend tag, the VKFFT_BACKEND the wrapper was built for, the device's cross-process identity, the SHA-256 of the wrapper library itself, the configuration size the wrapper reads, and every field of the plan's configuration except the two tuning fields. The device identity is the human-readable one:\n\nbackend identity\nOpenCL platform name, device name, CL_DRIVER_VERSION\nCUDA device name, compute capability\nMetal device name, registry ID\n\nA record that is absent, unreadable or unparseable is a miss, not an error. Writes go through a temporary file and a rename, so two processes writing the same key is benign and no reader sees a half-written record. A write that fails, on a full disk or a read-only depot, throws nothing.\n\ntune=:force re-sweeps one shape and overwrites its record, which is how a machine whose driver has changed underneath its records gets retuned. VkFFT.clear_tuning! deletes all of them.\n\nNothing compiled is ever cached. Two integers per tuned configuration is all that persists. VkFFT does have an interface for saving compiled applications, and this package does not call it.","title":"Records on disk"},{"category":"section","location":"limitations/#Limitations","page":"Limitations","text":"","title":"Limitations"},{"category":"section","location":"limitations/#Arrays-you-can-pass-to-a-plan","page":"Limitations","text":"You may only pass a dense device array of the backend you loaded. Passing a non-contiguous view, an adjoint, a permuted view and a host Array all throw an error.\n\nA contiguous slice of a larger allocation depends on the backend. There are no issues here with CUDA.jl. OpenCL buffers cross as cl_mem handles and Metal buffers as MTLBuffer objects, and neither carries an offset the launch parameters can use, so a CLArray or MtlArray at a nonzero offset is refused. You must copy the array first.\n\nhost = ComplexF32[i for i in 1:8]\nbig = CLArray{ComplexF32}(undef, 8)\ncopyto!(big, host)\n\nhead = view(big, 1:4) # offset 0\ntail = view(big, 5:8) # nonzero offset\n\np = VkFFT.plan_fft(head)\np * head # fft(host[1:4])\np * tail # ArgumentError\n\nThe layout is checked on every transform, not only at plan time, because a plan is reusable across buffers and an offset view of the right size clears every size check.\n\nA plan is also bound to the device context it was built in. Passing an array from another CUDA or OpenCL context, or from another Metal device, throws an error.","title":"Arrays you can pass to a plan"},{"category":"section","location":"limitations/#Precision","page":"Limitations","text":"Using the wrong element type will throw an error according to the table below:\n\nYou passed You should do instead\nA real array into VkFFT.plan_fft VkFFT.plan_rfft, VkFFT.plan_brfft, VkFFT.plan_irfft\nA complex array into VkFFT.plan_rfft or a real-to-real planner VkFFT.plan_fft\nA real array into VkFFT.plan_conv Convert first, since VkFFT's real-input convolution needs a padded in-place layout this package does not build\nAn Int or BigFloat array Use the element types VkFFT supports, see Element types (or VkFFT.unsafe_plan for quad precision)\n\nVkFFT.plan_conv throws an error for ComplexF16 on every backend. Upstream, VkFFT has issues with convolutions on half precision (it gives the wrong values), so we throw an error in this package.\n\nTwo more errors can happen relating to the device or the wrapper rather than the element type, and both are on Backends and capabilities. Half precision on OpenCL needs cl_khr_fp16, and half precision on CUDA needs a wrapper built against a CUDA toolkit.","title":"Precision"},{"category":"section","location":"limitations/#Bugs-in-VkFFT","page":"Limitations","text":"Each configuration below is one VkFFT technically accepts, reports VKFFT_SUCCESS for, but gives the wrong numerical output values. The planner throws an error for every one of them, so this list is here to explain the guards, not to warn you off anything you can still do:\n\nConfiguration What VkFFT does\nA region that transforms nothing, or whose every axis has length 1 Reports success and dispatches no kernel, leaving an out-of-place caller holding uninitialized memory. The planner applies it as a copy in Julia instead\nA type-1 DCT or DST of an axis of length 1 Never returns from vkfft_create. The internal transform length goes to zero and the radix decomposition has nothing to cancel. The other types create, run, and compute the identity where FFTW scales\nA DCT and a DST on one plan Computes the DST, or at other type pairs something that is neither while still round-tripping\nA real-to-complex plan carrying a DCT or a DST Corrupts the spectrum\nA DCT or DST type outside 1 to 4 Falls through every branch that generates the pre- and post-processing, leaving a plain real transform\nA plan that both convolves and prepares a kernel Reads the kernel flag first and ignores the other\nA convolution plan with only one direction's kernels generated Dereferences the half it was told to skip\nZero-padding on any axis but VkFFT's own first one Creates, runs and reads uninitialized memory\nA convolution over three or more axes, or over a layout that omits an axis Rejects a leading or trailing omit and accepts a middle one while computing something that is no convolution\n\nSee also Known failures.","title":"Bugs in VkFFT"},{"category":"section","location":"limitations/#Freeing-a-plan","page":"Limitations","text":"Every plan is freed by its finalizer, so nothing has to be freed by hand. VkFFT.unsafe_free! frees one now instead, at most once:\n\np = VkFFT.plan_fft(x, 1)\nVkFFT.unsafe_free!(p)\np * x                 # ArgumentError, this plan has already been freed\nVkFFT.unsafe_free!(p) # a second call is a no-op\n\nIt is unsafe for two reasons. Another reference to the same plan can still be applied afterwards, which throws rather than crashing but is still not what its holder expected. And a cached plan stays in the cache, so the next call that would have hit it gets a freed plan back. Call VkFFT.clear_cache! first if you mean to free everything.\n\nA plan holds references to its backend objects, because the context and the queue a VkFFT application was built against have to outlive it.","title":"Freeing a plan"},{"category":"section","location":"limitations/#Do-not-plan-inside-an-autorelease-pool","page":"Limitations","text":"This is for Metal only. VkFFT's Metal plan builder over-releases the strings it compiles its kernels from, so an Objective-C autorelease pool drained after a planning call dies inside objc_release and takes the process with it:\n\np = VkFFT.plan_fft(x)                    # fine\nMetal.@autoreleasepool VkFFT.plan_fft(x) # crashes when the pool drains\n\nVkFFT.jl never opens a pool around planning, so this only bites if you wrap the call yourself. Applying a plan is unaffected. Where Julia is embedded in a Cocoa application that drains a pool on the main thread, plan from a worker task. The over-release is an upstream VkFFT bug with a fix pending in DTolm/VkFFT#227.","title":"Do not plan inside an autorelease pool"},{"category":"section","location":"limitations/#The-raw-configuration","page":"Limitations","text":"VkFFT.unsafe_plan builds a plan from a VkFFTConfig you filled in yourself, forwarded to the wrapper unchanged. Two prototype arrays supply what a configuration cannot: the backend, the device context, the element types and the Julia array shapes an application is checked against. Their contents are neither read nor written.\n\nh = rand(Float32, 16, 8)\nx = CLArray{Float32}(undef, size(h))\ncopyto!(x, h)\n\nconfig = VkFFTConfig(fft_dim=2,\n                     size=ntuple(i -> i <= 2 ? UInt64((16, 8)[i]) : UInt64(0),\n                                 VkFFT.VKFFT_MAX_FFT_DIMENSIONS),\n                     dct=2, make_forward_only=1)\nraw = VkFFT.unsafe_plan(config, x, similar(x))\n\nArray(raw * x) == Array(VkFFT.plan_dct(x, (1, 2)) * x) # identical\n\nZero means VkFFT's default for every field, so a zeroed configuration plus fft_dim and size is a valid out-of-place complex-to-complex plan in single precision. Axes run fastest first, which is Julia's column-major order, and size slots past fft_dim have to be zero.\n\nunsafe_ means something specific: not one of the errors in Bugs in VkFFT runs on this path. The damage is not just a wrong answer. The transform VkFFT generates for a configuration like those is not the transform the buffers were sized for, so applying one writes past the end of them and corrupts the process heap, and the crash then lands wherever that heap is next touched. Work out the byte layout your configuration implies before applying it.\n\nA raw plan keeps the structural checks that are about Julia arrays rather than about the transform: the two shapes, the in-place agreement the configuration asked for, the memory layout of both arrays and the device context they live in. It has no inverse, no adjoint and no AbstractFFTs.fftdims, since nothing here knows which Julia dimensions config.size was built from. size is the input prototype's size and AbstractFFTs.output_size the output prototype's. Raw plans are never cached, and they are freed by their finalizer like every other plan.","title":"The raw configuration"},{"category":"section","location":"limitations/#Quad-precision","page":"Limitations","text":"VkFFT's precision = 3 is double-double: four native doubles per complex value, the real part as a high-order and a low-order double and then the imaginary part the same way. No native Julia element type has that layout, which is why quad is reachable only through the raw configuration. A ComplexF64 array of twice the length holds exactly those bytes, so it can stand in as a prototype, and the packing and unpacking are yours.\n\n# Only the high-order real double is written, which is exact: a Float64 is\n# its own double-double with a zero low-order part.\nfunction quad_pack(h::Vector{Float64})\n    flat = zeros(Float64, 4 * length(h))\n    for i in eachindex(h)\n        flat[4 * (i - 1) + 1] = h[i]\n    end\n    return collect(reinterpret(ComplexF64, flat))\nend\n\nn = 64\npacked = quad_pack(rand(n) .- 0.5)\nx = CLArray{ComplexF64}(undef, length(packed))\ncopyto!(x, packed)\ny = CLArray{ComplexF64}(undef, length(packed))\n\nconfig = VkFFTConfig(fft_dim=1,\n                     size=ntuple(i -> i == 1 ? UInt64(n) : UInt64(0),\n                                 VkFFT.VKFFT_MAX_FFT_DIMENSIONS),\n                     precision=3, make_forward_only=1)\nforward = VkFFT.unsafe_plan(config, x, y)\nmul!(y, forward, x)\n\nThe inverse is the same configuration with normalize=1 and make_inverse_only=1, passed to unsafe_plan with direction=1 (a keyword of VkFFT.unsafe_plan, not a configuration field). Metal has no double precision, so it has no double-double either.","title":"Quad precision"},{"category":"section","location":"backends/#Backends-and-capabilities","page":"Backends and capabilities","text":"VkFFT.jl uses CUDA through CUDA.jl, OpenCL through OpenCL.jl and Metal through Metal.jl. Planning, region mapping, and plan cache are the same on all three backends. The only difference is what element types a device can use, what a contiguous slice of a larger buffer is allowed to be, and a short list of transforms that have upstream bugs on Metal.","title":"Backends and capabilities"},{"category":"section","location":"backends/#Element-types","page":"Backends and capabilities","text":" CUDA OpenCL Metal\nFloat32, ComplexF32 yes yes yes\nFloat64, ComplexF64 yes yes on every device tested no\nFloat16, ComplexF16 only through a wrapper built against a CUDA toolkit only where the device reports cl_khr_fp16 yes\nDouble-double quad, through VkFFT.unsafe_plan yes yes no\nContiguous views at a nonzero offset yes no no\n\nNote that half precision in VkFFT is only a storage format. Arithmetic is still performed in single precision, though data is read and written in half precision.\n\nEvery transform listed in Transforms runs on all three backends. There are two exceptions: fused convolution for ComplexF16 is not implemented, and there is no real-input convolution (i.e., no r2r nor r2c convolution).","title":"Element types"},{"category":"section","location":"backends/#CUDA","page":"Backends and capabilities","text":"For half-precision, you need to build libvkfft yourself against a CUDA toolkit.\n\nA plan belongs to the CUDA context it was built in. VkFFT loads its nvrtc modules and allocates its scratch buffers in whatever context is current when the application is built. Every launch and the freeing of the plan have to happen in that same original context too. An array from another context is refused. Execution takes the stream from CUDA.stream() on every call (not the legacy default stream).","title":"CUDA"},{"category":"section","location":"backends/#OpenCL","page":"Backends and capabilities","text":"VkFFT drives clSetKernelArg with a cl_mem, so OpenCL.jl has to return buffer-backed arrays rather than its default unified-memory ones. You can do this by setting the default_memory_backend preference to \"buffer\":\n\nusing Preferences, OpenCL\nset_preferences!(OpenCL, \"default_memory_backend\" => \"buffer\")\n\nPassing a unified-memory CLArray to the planner throws an error.\n\nHalf precision is a per-device extension. Without cl_khr_fp16 VkFFT emits half arithmetic the driver will not compile. The planner checks the device's extension list first and informs you if it is not present.\n\nNote that the wrapper has to be linked against an ICD loader.\n\nA CLArray at a nonzero offset into its buffer is refused. You should copy such an array first.\n\nOpenCL also runs on CPU. Add pocl_jll to enable this:\n\nusing pocl_jll # must come before OpenCL\nusing OpenCL\nusing VkFFTOpenCL\nusing LinearAlgebra\n\n# Only needed when another ICD is installed. pocl calls itself this.\ncl.platform!(first(p for p in cl.platforms() if p.name == \"Portable Computing Language\"))\n\nh = rand(ComplexF32, 1024)\nx = CLArray{ComplexF32}(undef, size(h))\ncopyto!(x, h)\n\np = VkFFT.plan_fft(x, 1)\ny = p * x\nnorm(Array(inv(p) * y) - h) / norm(h) # Relative error should be < 1e-6","title":"OpenCL"},{"category":"section","location":"backends/#Metal","page":"Backends and capabilities","text":"Metal does not support double precision, so you cannot use Float64 and ComplexF64. This is a hardware limitation.\n\nPassing an MtlArray at a nonzero offset to a plan throws an error. Similarly, using an array on a different Metal device than the current task's throws.\n\nThere is one Metal-only crash to know about before you write anything around a planning call. See Do not plan inside an autorelease pool.","title":"Metal"},{"category":"section","location":"backends/#Known-failures","page":"Backends and capabilities","text":"On pocl, single-precision kernel generation fails at lengths 8191 and 4093. You should run these lengths in double precision on pocl.\n\nThe fused convolution is incorrect for a Bluestein axis long enough that VkFFT splits it across several uploads. The length at which the split starts depends on the device's shared memory, so there is nothing for the planner to compare against. Check one convolution against a host reference before trusting a transformed length with a large prime factor. Note that this is problematic only for large prime lengths and for fused convolutions only (i.e., Fourier transforms are fine).","title":"Known failures"},{"category":"section","location":"backends/#Tolerances","page":"Backends and capabilities","text":"The relative tolerances the test suite scores against FFTW, element by element against the peak magnitude of the reference:\n\n CUDA OpenCL Metal\nsingle precision 1e-4 1e-4 1e-4\ndouble precision 1e-12 1e-12 not applicable\nhalf precision 5e-3 5e-3 5e-3\n\nThe measured errors sit well inside those. On pocl the worst element-wise error over sizes up to 16k is 2.5e-5 in single precision and 4.9e-14 in double. On an M3 Pro it is 1.4e-5 in single precision, at 16381 through Bluestein, and 1.1e-3 in half precision over the complex, real and real-to-real families.","title":"Tolerances"},{"category":"section","location":"backends/#Twelve-axes","page":"Backends and capabilities","text":"VkFFT takes at most twelve axes per plan. Note that this is a choice made by the libvkfft wrapper, not a limitation of the original C++ library VkFFT.\n\nThe cap applies after the region is collapsed, not to ndims. Length-1 axes are dropped, adjacent untransformed axes merge into one, and a trailing run of untransformed axes folds into VkFFT's batch count instead of occupying an axis at all. So the only way to reach twelve is to alternate transformed and untransformed dimensions many times over, as in transforming every other axis of a 25-dimensional array, and the error tells you to permute the array so the transformed dimensions sit next to each other. You are extremely unlikely to hit this.","title":"Twelve axes"},{"category":"section","location":"backends/#Multiple-backends-need-multiple-processes","page":"Backends and capabilities","text":"The C++ VkFFT library chooses its backend when it is compiled. A Julia session can only drive one backend, whichever wrapper the libvkfft_path preference names. Loading VkFFTCUDA and VkFFTMetal into the same session does not give you a process that can do both. This is why this package's own Metal test suite runs in a child process. A program that needs two backends needs two processes.\n\nVKFFT_BACKEND is 1 for CUDA, 3 for OpenCL and 5 for Metal.\n\nThe first call that reaches the C library checks the wrapper's VKFFT_MAX_FFT_DIMENSIONS and its configuration size against the mirror compiled into VkFFT.jl. A wrapper and a package from different versions throws an error.","title":"Multiple backends need multiple processes"},{"category":"section","location":"#VkFFT.jl","page":"Home","text":"Julia bindings for VkFFT, DTolm's runtime-compiled GPU FFT library. Bindings are available for three backends: CUDA, OpenCL, and Metal.\n\nVkFFT implements complex-to-complex transforms, real-to-complex, complex-to-real, cosine and sine transforms of types I to IV, fused circular convolution, fused zero-padding, half precision, and quad precision. An autotuner is provided which picks VkFFT's block and thread hints per device and per shape for the best performance.\n\nVkFFT uses Bluestein's algorithm for axes with lengths that have a prime factor above 13. Thus, any size FFT can be done.","title":"VkFFT.jl"},{"category":"section","location":"#VkFFT-Backends","page":"Home","text":"You don't want to use VkFFT.jl directly. Instead, use one of VkFFTCUDA, VkFFTOpenCL or VkFFTMetal, depending on your device. See Backends and capabilities for what each backend is capable of.","title":"VkFFT Backends"},{"category":"section","location":"#Install","page":"Home","text":"<!-- TODO(jll): install instructions pending VkFFT_{CUDA,OpenCL,Metal}_jll\n     registration. Until then, the libvkfft_path preference is the only way\n     in. This block is rewritten by the JLL wiring wave. -->\n\nusing Pkg\nPkg.add(\"VkFFTOpenCL\") # or VkFFTCUDA, or VkFFTMetal\n\nVkFFT.jl calls libvkfft, a small C wrapper around VkFFT. There is no JLL for that wrapper yet, so it has to be built once and named in a preference:\n\nusing Preferences, VkFFT\nset_preferences!(VkFFT, \"libvkfft_path\" => \"/path/to/libvkfft.so\")\n\nVkFFT chooses its backend when it is compiled, so the wrapper is built once per backend: -DVKFFT_BACKEND=1 for CUDA, 3 for OpenCL and 5 for Metal.\n\nOpenCL needs one more preference, and each backend has a short list of its limitations. See Backends and capabilities for more information.","title":"Install"},{"category":"section","location":"#Quickstart","page":"Home","text":"using VkFFTOpenCL, OpenCL\nusing LinearAlgebra\n\nh = rand(ComplexF32, 1024, 64)\nx = CLArray{ComplexF32}(undef, size(h))\ncopyto!(x, h)\n\np = VkFFT.plan_fft(x, 1) # transform dimension 1, batch over dimension 2\ny = p * x                # or mul!(y, p, x)\nback = inv(p) * y        # normalized inverse, 1/N applied in-kernel\nnorm(back - h) / norm(h) # Relative error, should be < 1e-6 for single precision\n\ny is fft(h, 1) and back is h again, both to single-precision.\n\nThe same program on the other two backends changes two lines, the using and the array type:\n\nusing VkFFTCUDA, CUDA\n# ...\nx = CuArray{ComplexF32}(undef, size(h))\n\nusing VkFFTMetal, Metal\n# ...\nx = MtlArray{ComplexF32}(undef, size(h))\n\nVkFFT.plan_fft is module-qualified. This means that fft(x) and plan_fft(x) will call the function from whichever package owns the array type (i.e., CUFFT on a CuArray, MPSGraph on an MtlArray, and a MethodError on a CLArray, since OpenCL.jl has no FFT of its own).","title":"Quickstart"},{"category":"section","location":"#Read-more","page":"Home","text":"Backends and capabilities for details on the backends, what each device runs, and the setup each backend needs.\nTransforms for the entry points, their arguments and the size and layout rules.\nThe plan interface for how to use plans, how they cache data and the module-qualified entry points.\nTuning for the autotuner and its records.\nLimitations for the layout rules, the lifetime rules or the raw configuration.\nMigrating from VkFFTCUDA 0.2 if you used the old package.\nAPI reference for the docstrings.","title":"Read more"},{"category":"section","location":"migration/#Migrating-from-VkFFTCUDA-v0.2","page":"Migrating from VkFFTCUDA 0.2","text":"v0.3 is a rewrite. The bindings moved into VkFFT.jl and VkFFTCUDA became a loader: it depends on VkFFT.jl and CUDA.jl, and it re-exports VkFFT.jl. The package name and UUID did not change, so upgrading in place works.","title":"Migrating from VkFFTCUDA v0.2"},{"category":"section","location":"migration/#What-changed","page":"Migrating from VkFFTCUDA 0.2","text":"VkFFTCUDA 0.2 Now\nplan_fft(x), through a method on AbstractFFTs.plan_fft(::CuArray, region) VkFFT.plan_fft(x)\nfft(x) routed to VkFFT fft(x) routes to CUFFT. Write VkFFT.plan_fft(x) * x\nPkg.build(\"VkFFTCUDA\"), which cloned a repository and ran sudo make install Nothing to build at install time. Point the libvkfft_path preference at a wrapper\n__precompile__(false) Precompiles normally\nCUDA only CUDA, OpenCL and Metal\nComplex-to-complex only Complex-to-complex, real-to-complex, DCT and DST, fused convolution, fused zero-padding\nComplexF32 and ComplexF64 Those, plus ComplexF16 and double-double quad","title":"What changed"},{"category":"section","location":"migration/#The-entry-points-are-module-qualified-now","page":"Migrating from VkFFTCUDA 0.2","text":"Every planner gained a VkFFT. prefix:\n\nusing VkFFTCUDA, CUDA\nusing LinearAlgebra\n\nx = CuArray{ComplexF32}(undef, 1024, 64)\ncopyto!(x, rand(ComplexF32, 1024, 64))\n\np = VkFFT.plan_fft(x, 1) # was plan_fft(x, 1)\ny = p * x\nback = inv(p) * y\n\nA bare fft(x) or plan_fft(x) on a CuArray now goes to CUFFT. See Specify VkFFT.\n\nNothing else about a plan changed. You still get a first-class AbstractFFTs.Plan, so *, mul!, inv, \\, ldiv!, size, adjoint and AbstractFFTs.fftdims all work on it, on the new families too. See The plan interface.","title":"The entry points are module-qualified now"},{"category":"section","location":"migration/#There-is-nothing-to-build","page":"Migrating from VkFFTCUDA 0.2","text":"Pkg.build(\"VkFFTCUDA\") and the deps/build.jl behind it are gone. VkFFT.jl finds its wrapper through a preference instead, and will find it through a JLL once one is registered. See Install.","title":"There is nothing to build"},{"category":"section","location":"migration/#What-is-new","page":"Migrating from VkFFTCUDA 0.2","text":"New families, on Transforms: real-to-complex and back, cosine and sine transforms of types I to IV, fused zero-padding and fused circular convolution. ComplexF16 and Float16 plan like any other element type on a device that supports them, and double-double quad precision is reachable through the raw configuration.\n\nTuning covers the autotuner, which persists what it measures across processes.\n\nTwo new backends. The same code runs on OpenCL and on Metal through VkFFTOpenCL and VkFFTMetal, with the array type as the only difference. See Backends and capabilities.","title":"What is new"}]
}
